# dependencies / build output (extend with your stack's build dirs)
node_modules/

# test and analysis artifacts
coverage/
reports/

# secrets: NEVER in git (doctrine: wiki ops/07)
.env
secrets/*.env

# operations
backups/

# engram team memory: chunks + manifest ARE committed; the local DB is not
.engram/engram.db*

# os noise
.DS_Store
