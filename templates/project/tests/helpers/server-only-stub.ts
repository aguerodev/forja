// Empty stub aliased over the `server-only` package in vitest.config.ts.
// The real package throws at import time outside a React Server environment;
// unit tests exercise server modules directly, so it must resolve to nothing.
export {};
