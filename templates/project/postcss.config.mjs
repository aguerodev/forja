// Tailwind v4: CSS-first configuration. No tailwind.config.js exists —
// tokens and theme live in src/app/globals.css (@theme inline).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
